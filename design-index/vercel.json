{
  "version": 2,
  "framework": "react-router",
  "buildCommand": "react-router build",
  "outputDirectory": "build/client",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
